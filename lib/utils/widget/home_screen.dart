import 'package:flutter/material.dart';

class HomeScreenWidgeta extends StatelessWidget {
  const HomeScreenWidgeta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
